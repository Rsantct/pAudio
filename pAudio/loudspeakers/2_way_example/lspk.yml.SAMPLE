# See FullRange example for more info on DRC
drc:
    cabecero:
        type:    fir

#
# XO sets can be IIR or FIR
#
xo:

    # For FIR XO sets, just provide 'type: fir' and optionally 'flat_gain and or 'posit_gain'.
    # Then pAudio will populate the needed parameters for each way (hi, lo, ...)
    # by using the set name and searching for PCM files named like that.
    # under the loudspeaker folder. Example:

    rePhaseTest.mp:        # This assumes proper .pcm files named
                           # 'xo.lo.rePhaseTest.mp.pcm' and 'xo.hi.rePhaseTest.mp.pcm'
                           # (the free suffix .mp would indicate a minimum phase FIR, but has no effect)
        lo:
            type:   fir
            flat_gain:   -8.7
            posit_gain:   8.7
        hi:
            flat_gain:   -2.1
            posit_gain:   2.1


    # For IIR XO sets, provide each way (lo, hi) in separate sections, with its the 3 basic parameters. Example:
    
    iirTest:
        lo:
            type:   LR      # LinkwitzRelay or Butterworth
            order:  2       # must be even for LinkwitzRelay
            freq:   2000    # Hz
        hi:
            type:   LR
            order:  2
            freq:   2000

#
# The OUTPUTS section to map towards the SOUND CARD PHISICAL OUTPUTS
#
# (i) output index starts with `1` (to follow jack `system:playback_N` ports numbering)
#
outputs:
    
#   num     name    Gain dB    Polarity     Delay       bind to
#                              +/-          ms          system
    1:
    2:
    3:      lo.L    0.0         +           0.0         true
    4:      lo.R    0.0         +           0.0         true
    5:      hi.L    0.0         +           0.0         true
    6:      hi.R    0.0         +           0.0         true
