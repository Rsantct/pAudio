**THIS IS A DRAFT, PENDING TO REVIEW**

## 2 way example

All pcm files are fake ones, just to illustrate `.pcm` files naming

Xover gain, delay and polarity are set in _human readable_ format in the `outputs:` section inside the `lspk.yml` file.

